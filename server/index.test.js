const app = require('./app');

describe('User API Tests', () => {
  
    describe('GET Requests', () => {
      // All tests related to GET requests go here
        beforeEach(() => {
            jest.clearAllMocks();
        });
    });
  
    describe('POST Requests', () => {
      // All tests related to POST requests go here
        
    });
  });